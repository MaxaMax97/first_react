let initialState = {
    friends: [
        {id: 0, name: 'Maxim'},
        {id: 1, name: 'Igor'},
        {id: 2, name: 'Lena'}
    ]
}
const sidebarReducer = (state = initialState, action) => {
    return state;
}
export default sidebarReducer